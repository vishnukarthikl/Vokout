<header class='navbar navbar-inverse navbar-static-top'>
  <nav class='container' role='navigation'>
    <div class="navbar-header">
      <%= link_to "Vorkout", root_path, id: "logo", class: 'navbar-brand' %>

      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='.navbar-collapse'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
    </div>
    <ul class="nav navbar-nav navbar-right collapse navbar-collapse">
      <li><%= link_to "Home", root_path %></li>
      <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_owner.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Profile", current_owner_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
      <% else %>
          <li><%= link_to "Log in", login_path, id:'login' %></li>
          <li><%= link_to "Sign up", signup_path,id:'signup' %></li>
      <% end %>

      <li><%= link_to "Help", help_path %></li>
    </ul>
  </nav>
</header>